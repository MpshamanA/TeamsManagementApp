export type Store = {
  id: number;
  storeName: String;
  updateUser: String;
  updateTime: String;
  done: boolean;
};

export type User = {
  name: string;
  position: string;
  email: string;
  password: string;
};
